import 'package:flutter/material.dart';

class DetailSeparator extends StatelessWidget {
  const DetailSeparator({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.grey[200],
      width: double.infinity,
      height: 10,
    );
  }
}
